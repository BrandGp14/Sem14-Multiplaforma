name: laboratorio_crud
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # Paquetes para la base de datos local
  sqflite: ^2.3.3+1
  path: ^1.9.0
  # Paquete para formatear fechas
  intl: ^0.19.0
  sqflite_common_ffi: ^2.3.6
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true